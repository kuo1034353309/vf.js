import IEventObserver from '../event/IEventObserver';
export default interface ILogger extends IEventObserver {

    // mark：增加开关待定..
    // get enabled(): boolean; 
    // set enabled(value: boolean): void;

}
